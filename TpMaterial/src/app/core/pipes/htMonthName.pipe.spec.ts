import { HtMonthNamePipe } from './htmonthname.pipe';

describe('HtMonthNamePipe', () => {
  it('create an instance', () => {
    const pipe = new HtMonthNamePipe();
    expect(pipe).toBeTruthy();
  });
});
